# 文档审查整改记录

## 目标与范围

日期：2026-09-12。模式：整改交付。状态：已完成。

本次目标：消除路线、协议、安全和目录治理矛盾，补齐当前规划阶段必要的文档细节。三个方案子代理讨论后，由主对话统一修改；子代理只读讨论和复审，不编辑项目。

范围包括根说明、协作约定中冲突的目录规则、全局文档与四个模块入口。不进行功能开发、技术栈定案、外部联调、提交或发布。

## 项目画像

当前工作目录仅有 Markdown 文档，未发现运行源码、构建配置或测试工程；Git 状态命令返回“不是 Git 仓库”。规划组件为控制层、Windows Executor、MCP 入口及 RustDesk 会话；首期单执行器、单交互会话。运行语言、存储、配置格式和业务消息传输扩展点均未确认。

涉及数据包括桌面画面、调用身份、工具参数、确认决定和审计记录。规划风险是远程控制副作用、身份混淆、重放和敏感信息泄露；本次仅修改设计文档，不连接实际远端或执行工具动作。

## 方案讨论与决策

| 议题 | 采纳方案 | 依据及影响 |
|---|---|---|
| 路线编号 | 按实际依赖重排第六至第九阶段，独立第十一阶段 | 保留旧编号加发布前置的初稿仍使顺序退出条件产生歧义；现已重排为 MCP、RustDesk、受控写、高风险执行，并同步进度表 |
| 协议去重 | 消息定义集中到协议草案，工具目录另页 | 删除旧平面请求、旧错误和旧状态定义，避免仅添加权威声明而保留矛盾 |
| 安全边界 | 安全策略与消息格式分别维护 | RustDesk 传输认证不代替业务身份认证；幂等查询不等于副作用重放 |
| 新增文档 | 增加技术基线、协议、验证计划、目录入口及本记录 | 有具体决策、字段、门禁或证据才成文；快速启动与运维手册随实现补齐 |
| 目录冲突 | 保持全局与模块同级的现行结构 | 修正协作约定里另一套编号目录规则，不进行目录迁移 |
| 协议成熟度 | 保持草案，阻断未决项下的协议冻结 | 不编造认证算法、技术栈或历史客户端；完整 JSON Schema 随第一阶段冻结交付 |

## 实施与验收

已完成：方案讨论；导航、路线与协议草案修改；独立复审发现的进度依赖和缺失目录入口已修复；路线阶段已按依赖重排；协议与文档结构最终复审通过。

进行中：协议去重、失败边界补充、最终双重复审。

待完成：无。本记录保留未验证项，后续在对应实现阶段执行。

验收标准：所有内部链接可解析；每个有内容文档目录有入口；各阶段名称、顺序、依赖一致；字段、状态、错误无双重定义；实现与验证状态如实标记；复审无必须修复项。

验证证据：首轮相对链接与字面换行残留检查通过。当前结果不代表代码、密码学或 RustDesk 集成已验证，最终证据待补。

阻塞问题：文档修改无阻塞；协议冻结受技术基线中的未决项约束，由后续阶段负责人完成验证后解除。

下一步：进入第零阶段技术基线；实现前按协议冻结门禁补齐 Schema、算法和正反例验证。侧边任务复核已确认 RustDesk 与 Codex CLI 上游目录及当前提交，并已同步到 README、架构和技术基线；AgentDesk 集成仍未实现。

## 同步与回滚

本次所有业务文档均纳入同步清单，最终清单在验收时补齐。新增文档不是可执行交付物；产品第零阶段仍未完成。

没有 Git 历史可用于自动回滚，也未建立本轮修改前的完整文件快照。回退需依据工作区外已有备份或人工复原原文，不能宣称已验证自动回滚。协议尚无已发布实现，不涉及运行数据迁移。

