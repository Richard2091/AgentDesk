# 技术基线与待决策项

状态：规划。AgentDesk 当前目录有 Markdown 文档，未发现控制层或 Executor 代码骨架、依赖锁文件、构建配置、测试框架或站点构建配置；没有 AgentDesk 可运行的启动命令。控制层、Executor、MCP 和 RustDesk 业务消息集成尚未实现。

## 项目边界

首期为单控制层、单执行器、单 Windows 交互会话。计划处理工具参数、桌面画面、确认决定和审计记录。外部暴露面拟限于认证入口与绑定会话；实际监听方式、端口和部署拓扑待确认。已核实的上游源码目录为 `D:\Program\AIProject\rustdesk`（当前提交 `e82dd1235`）与 `D:\Program\AIProject\codex-cli`（当前提交 `aee8a55ab`）；版本锁定、许可证和允许修改范围仍待第零阶段确认。

技术选型按最小可复现原型验证，不预设语言、框架、数据库或部署服务。下表负责人是待分配的角色，不是已确认的人员。

## 决策与解除条件

| 决策 | 负责人角色 | 最迟阶段 | 解除条件 |
|---|---|---|---|
| 控制层/Executor 语言、进程和目录 | 工程负责人 | 第零阶段 | 验证跨进程结构化调用、构建、资源退出及 Windows 会话访问 |
| 依赖、上游版本与许可证 | 集成负责人 | 第零阶段 | 根据已核实源码目录固定 RustDesk/Codex 版本及获取方式，核对修改范围和分发约束 |
| 配置、密钥存储和环境隔离 | 安全与工程负责人 | 第一阶段冻结前 | 提供不含真实密钥的样例，明确引导、轮换、吊销和访问限制 |
| 业务认证、签名、参数摘要规范化 | 安全负责人 | 第一阶段冻结前 | 定义算法和格式，以有效、篡改、伪造、跨会话样例验证 |
| Schema 方言、字段/大小/时钟限制 | 契约负责人 | 第一阶段冻结前 | 所有消息及工具模式、正反例可由同一校验器执行 |
| 请求/令牌/审计存储、保留与事务 | 控制层及 Executor 负责人 | 第一阶段冻结前 | 决定幂等范围、崩溃后未知结果、令牌消费和墓碑清理策略 |
| 首期本地认证通道 | 集成负责人 | 第二阶段 | 验证认证、取消、断连及无副作用重放 |
| Codex CLI MCP 接入边界 | 集成负责人 | 第六阶段 | 以已核实 Codex CLI 的 MCP 客户端能力为入口，不修改其核心工具循环；完成身份、错误、取消和权限映射验证 |
| RustDesk 业务消息扩展和控制权 | 集成负责人 | 第七阶段前 | 固定扩展点，测量帧限制、排序、断连、背压及双向冻结 |
| 工具超时、资源与输出上限 | 能力负责人 | 每个工具注册前 | 实测正常/失败/清理及资源边界，补齐声明 |
| 监控、备份、发布与回滚 | 运维负责人 | 第十一阶段 | 完成测试环境演练与证据，明确恢复目标 |

## 启动文档的交付门禁

第零阶段落地代码骨架后提供实际依赖版本、配置样例、安装/构建/启动/停止命令、交互会话要求、最小演示和故障排查。新环境需复现启动两个组件和基础测试；不发布虚构的快速启动命令。

第一阶段提供可执行的 Schema 校验及正反例，业务运行闭环在第二阶段验收。运维手册随第十一阶段实际部署补充，至少包括配置、审计查询、监控、备份、升级、回滚及故障演练。各模块在形成特有设计或接口时新增专题文档；当前的候选范围记录在模块入口。

## 验证状态

以上决策与真实环境验证均未完成。[验证计划](../progress/validation-plan.md)保存对应验收场景，[开发进度](../progress/progress.md)记录阶段状态。协议草案可用于讨论，不能替代冻结决策。

