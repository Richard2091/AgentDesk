# AgentDesk 代理协作约定

本文档约束在本项目中工作的 AI Agent、自动化工具和协作开发者。项目当前处于早期规划阶段，文档、代码和验证结果必须保持一致。

## 工作原则

1. 先阅读与当前任务直接相关的文档，再修改文件。至少检查 `README.md`、`docs/global/governance/roadmap.md`、`docs/global/progress/progress.md` 以及相关模块文档。
2. 遵循已有需求、架构、安全边界和路线图；如果文档与实际工程问题冲突、存在不合理假设或会导致不可验证的方案，应明确指出，并说明依据、影响和建议方案。
3. 不把猜测写成事实。无法确认的技术选择、验收结果、风险结论和完成度必须标记为“待确认”或“未验证”。
4. 保持改动范围清晰。无关重构、格式化和文档改写不得混入当前任务。
5. 先建立轻量项目画像：记录当前任务涉及的组件、运行方式、入口、依赖、数据、权限、外部暴露面、风险和未知项；不得凭空假设技术栈、接口或数据结构。
6. 先明确目标、范围、非目标和验收标准，再拆分实施阶段。复杂任务应为每个阶段定义独立方案、测试要求、风险点和完成标准。

## 任务模式

根据用户意图选择工作模式：

- **实施模式**：用于新增功能、修复缺陷、重构、接入或配置变更。先调研现状，再形成变更计划，实施后执行质量门禁和独立复审。
- **审查模式**：用于检查、评估或判断是否合格。默认只读，不修改项目；问题必须说明位置、证据、风险、最小整改建议、验收标准和是否阻断发布。
- **整改交付模式**：用于明确要求修复已识别问题。按实施模式执行，并在修复后重新验证和复审。

如果任务模式或关键边界无法判断，先完成不依赖该判断的现状调研，并将待确认项写入进度记录。

## 文档同步要求

文档是项目交付物的一部分，不是开发完成后的补充材料。每推进一个阶段或形成重要决策，都要同步更新相关文档：

- `docs/global/progress/progress.md`：更新当前阶段、状态、完成度、已完成/进行中/待完成事项、阻塞问题、下一步和变更记录。
- `docs/global/governance/roadmap.md`：当阶段目标、交付物、验收标准、依赖关系或优先级发生变化时同步修改。
- `README.md`：当项目定位、目录结构、启动方式、初始范围或公开行为发生变化时同步修改。
- 专题文档：协议、架构、安全、Executor、适配器等内容发生变化时，更新对应 `docs/*.md`，并删除或修正过期描述。

专题同步范围包括：工具、消息和错误语义更新 `docs/global/contracts/`；组件职责和链路更新 `docs/global/architecture/`；全局认证、授权、确认、审计和副作用边界更新 `docs/global/security/`；模块特有内容更新对应顶层模块目录，例如 `docs/control-layer/`、`docs/windows-executor/`、`docs/mcp-adapter/` 和 `docs/rustdesk-session/`。

## 文档站点与目录治理

`docs/` 是在线文档站点的内容根目录。`global/` 与各产品模块目录必须同级；每个文档域和有内容的子目录都应有 `README.md` 作为入口和导航。文档使用 UTF-8、相对链接和稳定的小写目录名，目录层级原则上不超过四层。

全局文档维护跨模块唯一事实，模块文档只维护模块特有事实并引用全局文档。没有真实内容时不创建占位文件；只有模块形成独立职责、生命周期或验收边界时才新增子目录。重复内容必须合并到唯一权威文档，其他页面只引用。

模块文档建议按需增加 `design.md`、`contracts.md`、`testing.md`、`operations.md` 或 `decisions/`，不要求每个模块完整创建这些文件。重要且不可逆的架构决策使用编号 ADR 文件记录，普通实现选择写入设计文档。

文档目录按职责分层：`01-governance` 保存路线、范围和决策；`02-architecture` 保存总体架构和适配器设计；`03-contracts` 保存接口、消息、工具和错误契约；`04-components` 按控制层、Executor、适配器等模块继续扩展；`05-security` 保存安全设计和验证；`06-progress` 保存阶段方案、过程记录、验收和变更历史。新增模块文档优先放入 `04-components/<module>/`，模块接口契约放入 `03-contracts/<module>/`，模块安全说明放入 `05-security/<module>/`。

每个阶段至少完成以下记录：开始时将状态改为“进行中”；阶段结束时记录交付物、验证证据和验收结论；遇到阻塞时记录原因、影响、负责人或解除条件；阶段切换后更新下一阶段的前置条件。

## 实施与验证

1. 先建立可复现的最小变更，再扩展范围。实现、配置和文档中的名称、字段、状态和错误语义必须一致。
2. 对涉及安全、权限、确认、重放保护、审计、外部副作用或人工接管的改动，必须补充对应的边界说明和验证记录。
3. 运行与改动匹配的测试、构建或文档检查。无法运行时，记录原因、替代检查和剩余风险，不得宣称已验证。
4. 完成工作前检查版本控制差异，确认没有遗漏文档、临时文件、凭据或未经说明的行为变化。
5. 依据项目阶段运行适用的质量门禁：语法或编译检查、格式和静态检查、单元或集成测试、接口或契约测试、构建检查及依赖检查。没有代码或对应工具不可用时，记录替代检查和剩余风险。
6. 核心行为变更必须覆盖失败分支、边界条件、权限、超时、日志或指标，以及迁移和回滚影响；按适用性取舍，不机械增加复杂基础设施。
7. 涉及后端接口、消息协议、持久化、任务处理、权限控制或系统集成时，设计阶段必须说明接口形态、数据模型、状态流、并发、事务、错误处理、兼容策略和安全边界。

每次提交或交付前，列出受影响的文档，并对每个未更新的相关文档说明“无需同步”的理由。新增工具必须同时定义契约、风险等级、权限、超时、取消、审计、错误语义和测试；缺少其中任一项时不得宣称该工具已完成。

## 冲突与决策

当用户要求、项目文档和实际约束不一致时，优先保证安全性、可验证性和可维护性，并在回复中给出：冲突内容、实际影响、建议决策和需要用户确认的事项。除非冲突会阻止安全推进，否则先完成不依赖该决策的工作。

重要决策应记录在相关专题文档或 `docs/global/progress/progress.md` 的变更记录中，包含日期、背景、结论和影响范围。

## 复审与交付门禁

复杂、跨组件、高风险或涉及安全边界的任务，应安排独立复审；复审者只提出问题，不擅自修改主线产物。复审发现的问题分为“必须修复”“可选优化”“待确认”，必须修复项完成前不得交付。

阶段完成至少需要：目标范围已满足、实现或配置已落地、适用测试已通过、安全边界已验证、演示或启动步骤可复现、相关文档已同步。真实环境、真实接口、生产数据和外部服务测试不作为默认步骤，只有获得明确授权并具备回滚方案时才执行。

交付说明应包含：项目画像和适用规则、变更文件与行为变化、验证命令及结果、复审结论、文档同步清单、未验证项、剩余风险和回滚注意事项。

## 阶段记录模板

阶段推进时，在 `docs/global/progress/progress.md` 中至少记录：

```text
状态：未开始 / 进行中 / 已阻塞 / 待验收 / 已完成
本次目标：
已完成：
进行中：
待完成：
验证证据：
阻塞问题：原因、影响、负责人或解除条件
下一步：
```

只有在代码或配置、自动化测试、安全边界验证、可复现演示和启动文档均具备，且退出条件验收通过后，才能将阶段标记为“已完成”。

## 回复要求

完成任务时说明已修改的文件、行为变化、验证方式和未解决风险。涉及项目界面的选项、按钮、菜单和提示文本全部使用中文。

